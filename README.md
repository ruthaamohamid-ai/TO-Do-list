# 📝 Terminal To-Do List (Python)

## Description
This project is a terminal-based To-Do List application written in Python. It allows users to manage tasks directly from the command line by adding, viewing, and deleting tasks. Tasks are saved to a file so they persist across multiple program runs.

## Features
- Add new tasks
- View all saved tasks
- Delete tasks
- Persistent storage using file I/O
- Input validation to prevent invalid user entries

## Technologies Used
- Python
- PyCharm
- File I/O
- Lists and loops
- Command-line interface (CLI)

## How to Run
1. Make sure Python is installed on your system.
2. Clone this repository or download the project files.
3. Navigate to the project directory.
4. Run the program:
   ```bash
   python To-Do list.py